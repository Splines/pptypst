node_modules/

# Large vendored binaries fetched by `npm run assets` (see scripts/fetch-assets.mjs).
assets/vendor/

# Built bundles are regenerated by `npm run build`. Keep them out of git for now;
# drop this line if you want to commit the paste-ready files.
dist/
